sudo: required

language: java

services:
  - mysql
  - docker

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.m2

script:
  - bash travis.sh

notifications:
  email:
    recipients:
      - gabilampard@gmail.com

    on_success: always
    on_failure: always